# Xspeeria Financial Integrity Workflow
Amount/currency → authorization → validation → transaction → provider/bank event → state transition → settlement → reconciliation → audit trail. Verify idempotency and failure handling.
